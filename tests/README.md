# Root Level Tests

## Root Level Integration Tests

Tests that validate cross-cutting concerns, module interactions, and full system behavior.

- Full HTTP request/response testing
- Tests requiring the full application startup
- Tests involving multiple modules

## Architecture Tests

Tests that enforce module boundaries, dependencies, and architecture rules.
