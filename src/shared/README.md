# Shared

Contains packages which are relevant for all modules, covering

- Cross-cutting concerns
- Shared Abstractions (DTOs, interfaces, contracts, etc.)
- Shared Implementation (common functionality, utilities, etc.)
- Shared Web Components (e.g. React components, shared UI elements)
